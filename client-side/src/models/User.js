class User {
  constructor(firstName, lastName, email, birthDate, address) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.birthDate = birthDate;
    this.address = address;
  }
}

export default User;
